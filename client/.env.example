VITE_DJANGO_HOST=http://localhost:8001

# URL do backend do HUB (usado pra renovar a sessão — GET /session/token/refresh/).
VITE_HUB_HOST=http://localhost:8000

# URL do frontend do HUB (pra onde "Voltar ao HUB" redireciona).
VITE_HUB_FRONTEND_HOST=http://localhost:3000
